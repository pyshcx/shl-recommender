"""
helpers.py

Purpose:
    Miscellaneous utility functions shared across the project.
"""

def load_config(config_path: str) -> dict:
    """
    Loads project configuration from a file.
    
    Args:
        config_path (str): Path to the configuration file (e.g. YAML or JSON).
        
    Returns:
        dict: The parsed configuration properties.
    """
    # TODO: Open and parse the configuration file
    return {}

def setup_logger(name: str):
    """
    Sets up a standard logger for modules.
    
    Args:
        name (str): The name of the logger.
        
    Returns:
        Logger: A configured logging object.
    """
    # TODO: Configure standard logging format and level
    pass
